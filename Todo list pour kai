TODO:

Recherche



SELECT notifications.id, notifications.user_id, users.username, users.a FROM `notifications` INNER JOIN users ON notifications.user_id = users.id WHERE from_id = 1 AND content = "V" ORDER BY id DESC LIMIT 100

avatar
firstname
lastname
city
age
bio